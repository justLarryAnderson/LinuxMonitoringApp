ls -la /home/$USER | head -10
Что проверяет: Базовое выполнение команд с конвейером

ps aux | grep -v grep | grep -i "gnome\|kde\|xfce" | head -15
Что проверяет: Многоступенчатые конвейеры и фильтрацию процессов

find /home/$USER -name "*.txt" 2>/dev/null | head -8
Что проверяет: Обработку ошибок (2>/dev/null) и ограничение вывода

netstat -tuln 2>/dev/null | grep LISTEN | head -5
Что проверяет: Сетевые команды и фильтрацию состояний

ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head -7
Что проверяет: Расширенные опции ps и форматирование

echo "User: $USER, Home: $HOME" && ls $HOME/Desktop 2>/dev/null | head -3
Что проверяет: Работу с переменными окружения и комбинирование команд

for i in {1..10}; do echo "Line $i"; sleep 1; done
Что проверяет: Возможность остановки длительных команд кнопкой "Стоп"
